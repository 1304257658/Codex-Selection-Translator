{
  "name": "codex-selection-translator-standalone",
  "version": "0.6.0",
  "description": "A lightweight, LLM-free selection translator for the Codex desktop app.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/1304257658/codex-translation-plugin.git"
  },
  "scripts": {
    "check": "node --check hook.mjs && node --check renderer.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
